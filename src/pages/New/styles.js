import styled from "styled-components";

export const Container = styled.div`
  max-width: 70rem;
  height: 100vh;

  margin: 0 auto;

  a {
    margin-bottom: 2rem;
  }

  > header {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
  }
`;